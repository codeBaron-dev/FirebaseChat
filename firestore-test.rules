rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow any authenticated user to read/write any document (FOR TESTING ONLY)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}